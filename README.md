# Dimensionality Reduction Lab — Digits Dataset

## Run
```bash
pip install -r requirements.txt
streamlit run app/app.py
```

## Structure
```
combined/
├── app/app.py           # Combined PCA + t-SNE dashboard
├── data/digits.csv      # Digits dataset (1797×64)
├── notebook/
│   ├── pca_digits.ipynb
│   └── tsne_digits.ipynb
└── requirements.txt
```
